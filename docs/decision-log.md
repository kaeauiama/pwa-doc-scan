# Decision Log

新しいものが上。採番は D-001 から単調増加。番号は再利用しない。

| ID | 日付 | 決定 | 根拠 | 出所 |
|---|---|---|---|---|
| D-031 | 2026-08-25 | 撮影後は**必ず四隅の調整画面を挟む**。自動検出の結果をそのまま処理に流さない | 色帯のある原稿では検出を外すうえ確信度も下がらない(D-028)。確信度で分岐する設計だと取りこぼすため、常に人が見て直せる画面を通す。誤検出が致命的にならないのはこの動線があるため | Claude 提案 |
| D-030 | 2026-08-25 | Service Worker は**固定リストの precache をせず、取得した同一オリジンの GET を順次キャッシュする**方式にする | ビルドでファイル名にハッシュが付くため固定リストが維持できない。ビルド連携のプラグインを足すより、キャッシュ優先 + 裏で更新の単純な SW のほうが依存が増えず壊れにくい。初回だけオンラインが要る | Claude 提案 |
| D-029 | 2026-08-24 | 切り出し前に四隅を **0.6% 内側へ詰める**(`insetQuad`)。0 にもできる | 検出誤差は色つき原稿で最大 4.9px(短辺の 0.46%)出る。そのまま切ると縁に机が入り、白飛ばしや二値化が汚れを作る。実際のスキャナアプリも同じことをしている。**なお、当初これを「縁の汚れを直す修正」として入れたが、その汚れは可視化スクリプトの欠陥(明るさ 120 超だけ着色していた)による偽物だった。実出力に汚れは無い(最下端 30 行の黒率 0%)。検出誤差に対する予防として残す** | Claude 提案 |
| D-028 | 2026-08-24 | **カラーの帯がある原稿での誤検出は、現状では直さない。REQ-03(手動での四隅調整)で受ける** | 帯の境界が紙の縁より高コントラストになり、検出が帯に吸い付く(実測:下辺が 108px = 短辺の 10.3% 内側にずれる)。2 案を試して**両方とも失敗した**。(1) エッジのしきい値を 0.12→0.2 に緩める、(2) 「十分な支持がある中で最も外側」を優先する。64 ケースで測ると p90 が 15.35% → 26〜38% と大きく悪化したため revert した。画像の縁を拾い始めるのが原因。原理的な解法(色情報を使う、外側が紙でないことを確認する)は M4 以降で改めて扱う | Claude 判断(実測に基づく失敗の記録) |
| D-027 | 2026-08-24 | カラー / グレースケール出力では、**背景の白飛ばし(局所平均による正規化)を既定で掛ける**。OFF にもできる | 素の写真をそのまま PDF にすると紙が灰色や黄色に写り「スキャン」に見えない。二値化で使っている照明の平坦化(D-015)と同じ仕組みを、色を保ったまま適用する | Claude 提案 |
| D-026 | 2026-08-24 | 出力モードを **カラー / グレースケール / 白黒2値** の 3 種にする(REQ-20)。既定は白黒2値。カラーとグレースケールは **JPEG を PDF に DCTDecode で埋める** | チラシやカラーページ、写真の混じる原稿では二値化が破綻する。既存スキャナアプリはすべて 3 モードを持っており、これが無いと汎用のスキャンツールにならない。PDF 側は `/Filter /DCTDecode` でエンコード済みバイト列をそのまま埋めるだけなので、`core/` は純粋関数のままでいられる(JPEG 生成はアダプタの仕事) | **ユーザー起点**(実使用からの指摘) |
| D-025 | 2026-08-24 | **D-021 を撤回。解像度は切り替えず、常に最大解像度(12.2MP)で流す。** `switchOnCapture` は選択肢として残すが既定は false | 実機計測で最大解像度でも 30.2fps 実測・発熱なし。一方、切り替えは片道 約450ms かかり、その間に届いたフレームは 2 枚のみ(実質フリーズ)。往復で毎ショット約 900ms とプレビュー停止を払って得るものが無い。輪郭検出は縮小コピーに対して行うので、常時最大解像度でも負荷は増えない | 実機計測 → Claude 提案 |
| D-024 | 2026-08-24 | `getUserMedia` は**常に解像度制約を明示する**。既定値に頼らない | 制約なしで呼ぶと 640x480 が返ることを実機で確認した(capabilities の max は 4032x3024 なのに)。既定は最低解像度側に倒れる | 実機計測 |
| D-023 | 2026-08-24 | カメラは**起動時に自動で開かない**。「カメラを開始」の明示操作を置く | standalone では一度許可すれば同一起動中は再撮影できるが、**アプリを起動し直すと再度許可を求められる**(実機確認)。自動起動だと毎回いきなりプロンプトが出て体験が悪い。操作起点にすれば文脈が伝わる。HC-2(可視プレビュー時のみ撮影可)とも整合する | 実機計測 → Claude 提案 |
| D-022 | 2026-08-24 | **manifest(`display: standalone`)を M1 で必ず出す**。standalone 判定は `navigator.standalone` を第一に見る | 実機で `navigator.standalone === true` なのに `matchMedia("(display-mode: standalone)")` が false だった。probe に manifest が無いため。iOS では manifest が無いとホーム画面起動でも display-mode が browser のままになる | 実機計測 |
| D-021 | 2026-08-24 | プレビューは低解像度(1080p)で流し、**シャッター時のみ `applyConstraints` で 12.2MP に上げる**方式を第一候補とする。切り替え所要時間と黒画面の有無を実機で計測してから確定する | 12.2MP / 30fps を流し続けたときの発熱・電池が読めない(L-11)。ただし切り替えにカメラ再起動が挟まると体験を損なうため、実測前に確定はしない | **ユーザー承認済み**(試す方針) |
| D-020 | 2026-08-24 | **REQ-01 の 2 経路併存(D-002)を取り下げる。** 撮影経路は無音の `getUserMedia` 一本。`<input type="file">` は「**写真から読み込む**」として残す(高画質モードではない) | 実機で `getUserMedia` が静止画経路と同一の 12.2MP / 366dpi を返したため、高画質モードは「同じ解像度でシャッター音だけ鳴るモード」になっていた。一方、すでに撮影済みの写真を読み込む用途は解像度と無関係に価値がある。無音であることは実機で確認済み | **ユーザー承認済み** |
| D-019 | 2026-08-24 | `ImageCapture.takePhoto()` は**使わない**。定義は存在するが、静止画撮影 API を通ると日本版でシャッター音が鳴る可能性がある(L-10) | `getUserMedia` でセンサーのフル解像度 12.2MP が取れる以上、使う理由が無い。検証すらせずに避けるのが安全側 | Claude 提案 |
| D-018 | 2026-08-24 | probe の WebAssembly SIMD 判定バイト列が不正だったため修正。実機の「SIMD 非対応」は**計測ミスであり事実ではない** | Node 24(SIMD 対応)でも false が返ることを確認した。正しいバイト列では true。誤った計測値を根拠に設計判断をしないこと | Claude(自己修正) |
| D-017 | 2026-08-24 | 実機計測(iOS 18.7 / Safari 26.6)により **L-1(フル解像度が撮れない)と L-2(torch 不可)を取り下げる**。`getUserMedia` は 4032x3024 / 12.2MP / A4 実効 366dpi を返し、`torch: true` も出る | 机上調査の出典(2024〜2025 年の記事)と実機が食い違った。2026 年 8 月時点の実機を正とする。この結果により D-002(2 経路併存)の前提が消えたため、再判断が必要(U-09) | 実機計測 |
| D-016 | 2026-08-24 | 輪郭検出の作業解像度を **320px** とし、その後 **960px で各辺を当て直す 2 段構え**にする | 480px だと本文の行が Hough 上位を占め、紙の縁が 100 位以下に沈むことがある(実測)。320px まで落とすと文字が潰れて安定するが、隅の精度が 10.3px まで落ちる。高解像度で辺の近傍だけ再フィットすると 0.8px に戻る。robustness と精度を別の段で取る | Claude 提案(実測に基づく) |
| D-015 | 2026-08-24 | 輪郭検出の前段に **照明の平坦化(局所平均での除算)** を必ず入れる | これが無いと、影で暗くなった側の紙の縁のコントラストが明るい側の文字を下回り、全画面一律の分位点しきい値で縁だけが落ちる。実測で shading 0.3/0.5/0.7 の差が完全に消えた | Claude 提案(実測に基づく) |
| D-014 | 2026-08-24 | 輪郭の自動検出(REQ-02)を **OpenCV.js を使わず自前実装**する。Sobel + Hough 直線変換 + 対辺選択 + 高解像度での再フィット。**D-003 で選ばれた手段からの変更**(目的である「M1 から自動検出」は達成) | 実装して計測した結果、48 ケースのスイープで失敗 0・隅誤差の中央値 0.10% / p95 1.00% が得られた。OpenCV.js カスタムビルド(D-008 の目標 2.5MB)を入れずに NFR-02(初回 3MB)を大きく下回れる。純粋関数なので実機なしでテストでき、閾値も自分で調整できる。**U-05(OpenCV ビルドの選択)は不要になった** | Claude 判断・**要ユーザー確認** |
| D-013 | 2026-08-24 | リポジトリ名・プロジェクト識別子を **`pwa-doc-scan`** とする(旧 `super-camera`)。公開リポジトリとするため README に用途と Hard Constraints を明記する | 用途が名前から分かる。GitHub Pages の base は `/pwa-doc-scan/` になり、ワークフローがリポジトリ名から自動で入れるためコード変更は不要。公開する以上、HC-1/HC-2 の線引きを README にも書くのが HC-4(説明する)と整合する | **ユーザー承認済み**(README 追加は Claude 提案) |
| D-012 | 2026-08-24 | テストは **Node 24 の型ストリッピング + `node --test`** で走らせる。vitest 等のテストランナーを導入しない | ランナー依存ゼロで CI が軽く、`core/` が本当にデバイス非依存であることが構造的に担保される(Node で動かないコードは即座に落ちる)。制約として enum / namespace / パラメータプロパティが使えない | Claude 提案 |
| D-011 | 2026-08-24 | **ports / adapters でデバイス境界を分離**する。`core/` は `ports/` すら import しない。`FixtureImageSource`(合成書類)と `MemoryExporter` を用意し、実機なしでパイプライン全体を検証できるようにする | ユーザーの指摘(実機なし検証の構成)を受けて構造化。M0 で検証できたのは偶然デバイス依存が無かったからであり、それを設計で保証する。20-architecture.md §3 に「実機なしで検証できない項目」を明示した | **ユーザー起点**(Claude が構造化) |
| D-010 | 2026-08-24 | ホスティングは **GitHub Pages**(U-04 を確定)。`main` への push で Actions がテスト → ビルド → デプロイ。Vite の `base` は `BASE_PATH` 環境変数でワークフローから渡す | ユーザー希望。probe は `public/probe/` に置き `dist/probe/` へコピーされる | **ユーザー承認済み** |
| D-009 | 2026-08-24 | `probe/index.html` に実装した 1bit PNG / 1bit PDF エンコーダを **M1 コアの原型として採用**する。検証済み(10-platform-findings §4.1) | 自前 PDF 生成が現実的であることを M0 の時点で実証できた。CRC32 + `CompressionStream` + 最小 PDF ライタで完結し、外部ライブラリは不要。pypdf でのパースも通る | Claude 提案 |
| D-008 | 2026-08-24 | OpenCV.js は**フルビルドではなく `imgproc` 中心のカスタムビルド**(目標 2.5MB 以内)を前提とする。dnn / features2d / video / photo / objdetect は落とす | 必要なのは cvtColor, GaussianBlur, Canny, findContours, approxPolyDP, warpPerspective のみ。フルビルド 8〜10MB は NFR-02(初回 3MB)を単独で破る | Claude 提案 |
| D-007 | 2026-08-24 | コア処理(二値化・射影変換・1bit パッキング・PNG/PDF エンコード)は**依存ゼロの純粋関数**として実装し、単体テストを持つ(NFR-04) | 画質とサイズの要になる部分。ライブラリに隠すと調整もテストもできない | Claude 提案 |
| D-006 | 2026-08-24 | Hard Constraint は「実装しない」だけでなく**ガード + 回帰テスト**を対にする(00-scope §7) | プレイブックの指摘。制約が文章だけだと後で溶ける | Claude 提案 |
| D-005 | 2026-08-24 | 保存の主動線は**都度、共有シート**(`navigator.share({files})`)。アプリ内ライブラリ(OPFS 蓄積)は M1 では持たない | iOS で最も確実に動く経路。端末内にデータを溜めないため消失リスク・プライバシー懸念が最小。複数ページ対応(REQ-10)は一時保持のみで実現する | **ユーザー承認済み** |
| D-004 | 2026-08-24 | 出力形式は **1bit 画像を FlateDecode で埋めた PDF を第一級**とし、自前生成する。PNG 画像・グレースケール JPEG は M1 では出さない | A4 1 ページ 20〜60KB を狙える。`CompressionStream('deflate')` の出力は zlib ラップ済みで PDF の FlateDecode にそのまま入るため、外部ライブラリ不要 | **ユーザー承認済み** |
| D-003 | 2026-08-24 | **自動輪郭検出を M1 に含める**(OpenCV.js を初期から導入)。Claude の当初推奨は「M1 は手動 4 隅から」だったが、ユーザー判断で自動検出込みとする | 自動検出が無いと「ただのカメラ」であり、既存アプリに対する体験差が出ない。リスク(初回 DL 量・M1 長期化)は D-008 のカスタムビルドで緩和する | **ユーザー承認済み**(Claude 推奨とは異なる選択) |
| D-002 | 2026-08-24 | 撮影経路を **2 つ併存**させる。既定は無音モード(`getUserMedia`)、切替で高画質モード(`<input type="file" capture>`、シャッター音あり) | `getUserMedia` は実効 130〜260dpi 見込みで小さい文字に不足する可能性がある。一方 system camera は 12MP(約 365dpi)が確実に取れる。どちらか一方では用途を取りこぼす | **ユーザー承認済み** |
| D-001 | 2026-08-24 | 無音撮影に関する一線を先に引く。HC-1(用途を書類に限定)・HC-2(隠し撮りを容易にする機能は恒久非対応)・HC-3(端末外へ送信しない)を Hard Constraint とする | 日本でのシャッター音は法律ではなく自主基準だが盗撮防止の背景がある。無音は「静かな場所で書類を撮るための副次的性質」として扱い、訴求点にしない | Claude 提案 → ユーザー了承(異議なく設計に組み込み) |

## UNDECIDED

`00-scope-and-constraints.md` §9 を参照(U-01〜U-06)。ここでは重複させない。

## 暫定値(Claude 提案・変更容易)

`00-scope-and-constraints.md` §10 を参照。数値の最終的な正はコード側の `src/config.ts`(予定)。
